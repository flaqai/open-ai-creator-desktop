export default function useCheckPaymentAndUpdateUserinfo() {
  // The open-source template has no account or payment backend. Keep this hook
  // as a compatibility no-op for consumers that still import it.
}
